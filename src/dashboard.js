import react from 'react';
import ReactDOM from 'react-dom';
import './dashboard.css';
import App from './App';

function dashboard() {
    // display dashboard
    // agenda view with date of today, tmrw, and day after tmrw
    // if matches date of any question in Notion database, display
    
    // session number only increases when a problem is marked done
    // TODO: what to do with session number?

    
}